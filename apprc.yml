_default:
    app:
        root_dir: ${cwd}
        var_dir: ${this.app.root_dir}/var/${mode}
    bob:
        dir: ${this.app.var_dir}/bob
test:
dev:
prod:
    app:
        root_dir: ${exedir}
